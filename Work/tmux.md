tmux command examples:

create new tmux session:
`tmux new -s updates`

Attach to lost session:
`tmux a -t updates`

List sessions:
`tmux list-sessions`

Split pane horizontal:
`Ctrl + b %`

Split pane vertical:
`Ctrl + b "`

Switch pane:
`Ctrl + b <arrow>`